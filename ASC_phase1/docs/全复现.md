# 初始化环境
```bash
conda init
conda create -n ASC01 python=3.12.0 -y
conda activate ASC01
```
# 下载模型

```shell
pip install modelscope
```

```python
from modelscope import snapshot_download

model_dir = snapshot_download('deepseek-ai/DeepSeek-R1-Distill-Llama-8B',cache_dir='./')
```

```shell
python download.py
```
# 环境依赖下载
```txt
transformers==4.50.1
torch==2.5.1
datasets==3.2.0
numpy==1.26.4
```

在此时加速，再下载，可以很快
```bash
source /etc/network_turbo
pip install transformers==4.50.1 torch==2.5.1 datasets==3.2.0 numpy==1.26.4
```

# flash_attention下载
```shell
wget https://github.com/Dao-AILab/flash-attention/releases/download/v2.8.3/flash_attn-2.8.3+cu12torch2.5cxx11abiFALSE-cp312-cp312-linux_x86_64.whl

pip install flash_attn-2.8.3+cu12torch2.5cxx11abiFALSE-cp312-cp312-linux_x86_64.whl
```

# 下载附加的一些包
```shell
pip install accelerate==0.26.0
pip install antlr4-python3-runtime
```


> [!NOTE] 学术资源加速
> 加速：`source /etc/network_turbo`
> 取消加速：`unset http_proxy && unset https_proxy`


> [!INFO] 踩过的坑
> 下载下来的模型少了文件 `tokenizer.json`
